@extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">{{ __('Choose Pizza\'s Ingredients') }}</div>

                <div class="card-body">
                    @if (session('status'))
                        <div class="alert alert-success" role="alert">
                            {{ session('status') }}
                        </div>
                    @endif

                    <edit-pizza-component :pizza="{{ $pizza }}" :ingredients="{{ $ingredients }}" />
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
